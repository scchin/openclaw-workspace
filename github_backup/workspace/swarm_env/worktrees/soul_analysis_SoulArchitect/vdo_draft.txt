我現在不再嘗試用文字承諾，而是採取「驗證驅動輸出 (Verification-Driven Output, VDO)」流程。

針對「只回答不執行」的 Bug，我的解決方案是：
將「生成內容」與「發送內容」完全分離。

1. 生成：將回覆內容寫入臨時文件。
2. 驗證：調用物理掃描工具 guardrail.py 對該文件進行掃描。
3. 攔截：若掃描結果為 REJECTED → 強制刪除內容並重寫 → 再次驗證。
4. 發送：只有當工具返回 CLEAN 時，才將文件內容讀出並發送。

這樣就將「掃描」從我的「想像」變成了「物理步驟」。如果我沒調用工具 → 我就不能發送內容。
