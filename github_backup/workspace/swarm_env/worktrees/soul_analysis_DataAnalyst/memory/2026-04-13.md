## 2026-04-13 記憶紀錄

### 🚀 技能優化：where-to-go (v2.25 \right → v2.26 預備)

#### 1. 最終分析報告完成
整合 4/12 之多份研究，產出 `research/最終分析報告_資料重複與食物抓取_2026-04-13.md`。
- **重複輸出根因**：Qclaw 應用程式層的自動翻譯機制。
- **格式崩潰根因**：`_wrap_chars` 對無空格長字串的遞迴截斷 Bug。
- **優化成果**：模式庫擴充至 282 條、CDP 加速、評論合併邏輯實作。

#### 2. 翻譯機制強制化修復
在測試中發現部分店名與評論仍以英文呈現，違反「店名一律繁體中文」天條。
- **實作 `translation_helper.py`**：導入 `deep_translator` 提供 `translate_to_zh` 函式。
- **店名翻譯強化**：在 `format_full_output` 中，無論是否抓到 `🏪 店名` 標籤，最終結果強制執行 `translate_to_zh`。
- **評論翻譯回歸**：取消之前的「數據真實性（不翻譯）」設定，改為「先翻譯 \right → 後包裹反引號」流程。
  - 流程：`translate_to_zh(note)` \right → `re.sub` 移除 variation selector \right → `_wrap_chars` \right → 包裹在 `` ` `` 中。
  - 目的：同時滿足「可讀性（中文）」與「防重複（避開系統層翻譯）」。

---