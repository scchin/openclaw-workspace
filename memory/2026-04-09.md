## 🛡️ Google Rate Limiter 強化更新 (2026-04-09)
- **問題診斷**：發現 `google-rate-limiter` 在 Gateway 重啟或 Session 重啟後未能自動激活，導致在頻繁請求時觸發 Google API 500 內部錯誤及後續 429 配額限制。
- **邏輯修正**：修改 `~/.openclaw/hooks/google-model-guard/handler.ts`，將觸發條件從單純的 `session:patch` 擴展至：
  - `session:start`：新會話啟動時立即檢查初始模型。
  - `config` / `gateway:restart`：當 Gateway 執行服務層級的操作（如配置應用、重啟）時，自動讀取 `openclaw.json` 中的 `primary` 模型，若為 Google 系列則強制啟動護盾。
- **效果**：實現了「服務層級重啟 \right → 自動檢查模型 \right → 自動激活保護」的閉環，減少手動激活的需求。

## 宵夜需求與 where-to-go 技能問題 (2026-04-09 晚)

- **用戶需求**：22:16 用戶要 15 間宵夜店
- **執行結果**：技能跑了 7 分鐘無輸出，程序進程仍 running，可能死在 CDP 階段或 goplaces 進程
- **同時疊加的問題**：
  - `google-rate-limiter` 在此期間接連觸發 429 + 500 錯誤（`api rate limit reached` / `internal error encountered`）
  - Gateway log 顯示連續 `[agent/embedded] embedded run agent end: runId=... isError=true model=gemma-4-31b-it provider=google error=⚠️ API rate limit reached`
  - `auth profile failure state updated: ... reason=rate_limit window=cooldown` → 表示 OpenClaw 內建 failover 機制有在作用，但護盾仍未被呼叫

## google-rate-limiter 技能更新 (2026-04-09 深夜)

- **更新進度**：技能已全面更新（SKILL.md + rate_guard.py），並確認 `handler.ts` 已支援 `session:start`、`session:patch`、`config`、`gateway:restart` 四種觸發
- **record-500**：已新增 `record_500()` 函式與 CLI 命令
- **技能失效根本原因**：`rate_guard.py check` 從未被 Gateway/Agent 在實際 API call 前主動呼叫，護盾處於被動監控模式（advisory），只能建議、不能攔截
- **防護缺口**：當 429/500 錯誤發生時，Gateway 報錯返回 Agent，但 Agent 來不及在錯誤後呼叫 `record-429`，整個補救流程中斷
- **待實現的目標**：當收到 `"API rate limit reached"` 或 `"Internal error encountered"` 時，自動執行 `record-429/record-500` + `check` + `sleep` + 自動重新發起請求，直到工作完成

## where-to-go 技能使用注意

- 執行命令：`python3 ~/.openclaw/skills/where-to-go/scripts/run.py "宵夜" --count 15`
- 已知問題：有時 CDP 階段或 goplaces 進程會卡住不回應，導致無輸出
- 目前等待時間設定：`sleep 2 &&` 仍遠遠不夠覆蓋 rate guard 的 15s 等待窗口
- **建議**：在 rate limiter 完全生效前，每次 call 前都要先 `check` 並等待

## 測試驗證 (2026-04-09 23:24)

- 手動執行 `record-429` → waitSecs: 15，狀態: backoff，正常
- `check` 回傳 2.0s（因距離上次 `record-429` 已有一段時間）
- `record-500` 仍存在於狀態中（`consecutive500: 1`）
- `state.json` 已更新：`last429Ts: 1775748331.873525`，`consecutive429: 1`